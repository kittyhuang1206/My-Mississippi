# My Mississippi assets

- `photos/` contains destination images that were previously embedded as base64 data inside `index.html`.
- `favicon.svg` is the site icon.

Photo captions, credits, source links, gallery order, and destination associations remain in `index.html` so the existing renderer and attribution UI continue to work.
